name: Tests

on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  dockerfile-test:
    runs-on: ubuntu-20.04
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Test image
        run: make test
